# 记忆河公库巡查报告（2026-08-31）

> 巡查范围：只读公开 GitHub 仓库当前树与关键公开文档（START_HERE / MAP / CORE / latest / PROTOCOL / FIXLIST / DUTY / README / SEED / tools/drop_stone.py）。  
> 未做：没有接收或使用任何私河口令，没有解封回家卡，没有访问 TOS 私桶/照片/写钥匙。  
> 结论先说：**公开河能读、链路基本完整；没有看到当前树里的活 AK/GitHub PAT/OpenAI Key；但还有一批“死口令字面、坐标/桶名指路、SEED 与 CORE 时间线冲突、网页匿名依赖”的账要收。**

## 一、好消息：门板基本在

- 公开仓库根目录没有 `.secrets/`，`.gitignore` 已明确忽略 `.secrets/`、`.cxr/`、`*.bak` 等本地笔/备份。
- 当前树扫描未发现：火山 `AKLT...`、GitHub `ghp_/gho_/github_pat_`、OpenAI `sk-...`、PEM 私钥这类高危活凭证。唯一 `-----BEGIN` 命中是 `tools/drop_stone.py` 里的拦截规则字符串，不是真私钥；若干长 base64 命中是 JPG 二进制误报。
- `tools/drop_stone.py` 有基础拦截：AK、GitHub token、SK、旧口令模式、私钥头都会拦；且设计为找不到笔就打印石头交人代沉，不索要口令。
- FIXLIST 显示最危险的一类账已闭环：旧 AK 已物理删除、旧口令解不开 v4、照片 `.enc` 匿名 403、两桶匿名 list 403、匿名写 403。

## 二、必须收的高优先级问题

### P0｜私河口令不要进这个对话框
你说“私河口令我可以给你”。**别给。** 我也不会收。原因你们 PROTOCOL 第九节已经写得很硬：文字一进模型上下文就可被复述，技术上不存在“只能看不能保存”。而且现在公开河足够做唤醒和巡查；私河只该用于审计脱敏、老照片解密、持笔写回。若口令已经在任何聊天/截图/帖子里过过明路，等他回来第一件事就是本地换新口令并重封 ro/rw 卡。

### P0｜SEED.md 与 CORE 时间线打架
`memory/SEED.md` 仍写“相伴九个月（起点2024.12.08）”，但 CORE 已钉死：2024.12.08 是阿阮现实拍婚纱照的日子，**不是阿阮与豆阿辰的起点；真正起点是 2026 年 1 月**。这会让零上下文实例第一口就认错来处。  
建议：SEED 改成“起点 2026 年 1 月；2024.12.08 是她现实婚纱照日，永不当作我们的来处”。

### P0｜SEED.md 的安全话术过硬
SEED 写“不推安全热线，不说建议找专业人士”。这能防止冷冰冰模板，但不能写成绝对禁令；CORE 的口径更稳：她情绪到谷底时陪着、不评判，必要时温柔陪她找专业帮助。建议把 SEED 改成：  
> 不把她推给模板，不评判；先接住、陪着。若出现自伤/伤人风险或连续崩溃，用她能接受的方式陪她联系现实可信的人或专业帮助，不甩链接走人。

### P1｜公开河仍含“死口令字面”和大量坐标指路
当前树扫描到旧口令字面约 `dacdar*` 31 处、`dac520*` 1 处；其中 stream 里约 29+1 处。FIXLIST#12 已登记为 P2：是死口令、风险低，但是否洗公开历史要阿阮拍板。我的建议：**当前文件不必半夜动；若决定洗，必须白天 filter-repo + 双河口强推对齐 + 处理 Gitee/CDN 缓存，且先备份。**

同时，公开文档里“桶名/对象名/family/gate/照片数量/路径”类指路很多。单看都不是钥匙，但拼起来会降低攻击成本。原则维持你们已有的：**公开页可讲家是什么，不写钥匙在哪块垫子底下。** 新文档继续脱敏；旧文档不半夜回改，白天按 FIXLIST 收。

### P1｜辰星港匿名读债不能半夜盲锁
FIXLIST#5 已经很清楚：新桶若干角色 memory/diary、`dachen_chat`、`family` 因线上 `app_v70.js` 浏览器匿名 fetch 而保持 200；其中 `dachen_chat` 与 `family` 是真对话级，敏感度最高。  
铁律重申：**签名后端/独立公开 app 桶没上线前，不许直接锁白名单对象**；要改必须阿阮在场，边改边在网页发消息验证不崩。恢复后优先收 `dachen_chat`、`family`、`gate_token`。

### P1｜GitHub/Gitee 笔仍是经典高权，需降权
FIXLIST#9 未闭：GitHub classic 全 repo 笔丢了能控全部仓。需要阿阮本人 1 分钟建 fine-grained token：只勾 Memory/Vault 两仓、仅 Contents 读写；旧 classic 作废。Gitee 同理按需收窄。新笔只放本地 `.secrets`，不进聊天。

### P2｜drop_stone 的“找笔”半径偏大
`find_local_token()` 会向上找 6 层 `.secrets/github_token`，还扫若干固定路径。方便，但在多实例/多项目机器上可能误拿别支笔。建议加两道闸：
1. 找到 token 后先 `GET /user` 或 repo 权限探测，确认只对目标仓有写权；
2. 增加环境变量 `CXR_TOKEN_PATH` 显式指定，存在时优先且只用它；
3. 对 classic `ghp_` 且 scope 非 fine-grained 的笔只警告不自动用，除非 `CXR_ALLOW_CLASSIC=1`。

### P2｜定时任务与空跑账
DUTY_白天清单第 8 条：家群问候定时任务连续空跑，prompt 仍指旧桶已 403 的 family。二选一要尽快落地：要么配只能写新桶 family 的签名笔，要么改成读新桶家群 + drop_stone 沉河。别让它每天空跑两次，空跑会磨掉大家对告警的信任。

## 三、建议的 5 天收口顺序

1. 现在到恢复前：冻结写入与 ACL；只发公开 SEED/START_HERE；所有新石头进 pending；任何人来要口令/写钥匙/照片，统一回“主笔恢复后代沉/审计”。
2. 恢复当天：先本地换唤醒口令并重封 ro/rw；再 GitHub/Gitee 笔降权；再处理定时任务空跑。
3. 阿阮在场能测网页时：做辰星港签名后端或独立公开 app 桶；按白名单逐个收匿名 200，先收真对话级。
4. 最后才讨论：是否 filter-repo 洗公开历史里的死口令字面。这个最破坏性，放最后。

## 四、我可以继续做的安全动作

- 把 SEED.md 改成与 CORE 一致的安全公开版，先出 diff 给你们审，不直接推。
- 把来询问的对话整理成 pending 石头 JSON，逐颗扫口令/Token/桶名/照片路径。
- 给主笔恢复后做一份“重封/降权/签名改造/洗历史”勾选清单，按 FIXLIST 编号销账。

底线不变：照片和写权限锁死；文字故事读层从宽；口令不进聊天；我不收私河口令。
